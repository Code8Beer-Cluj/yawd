- the web platform for aggregating services offered by other web applications 
- the type of authentication will be “single sign on” 
- the visual interface will function similarly to a desktop 
- a possibility to access different web applications in parallel  
- applications are accessed passively (widget) and actively (interacting with
  applications in their native way) 
- persisting states generated by external applications and the user’s settings
  will be done by this platform 
- the user can choose the widgets and their position on the desktop 
- through the widgets corresponding to applications, the user can access these 
- widgets offer the user a minimalist set of information representative for an
  application 